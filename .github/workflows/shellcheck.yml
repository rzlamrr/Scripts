name: shellcheck
on:
  push:
    branches:
    - 'master'
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: cek
      uses: bewuethr/shellcheck-action@v2
